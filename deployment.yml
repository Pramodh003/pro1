apiVersion: apps/v1
kind: Deployment
metadata:
  name: spam-app1-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spam-app1
  template:
    metadata:
      labels:
        app: spam-app1
    spec:
      containers:
      - name: spam-app-container
        image: akash300/spam:1
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: spam-app1-svc
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 32002
  selector:
    app: spam-app1
